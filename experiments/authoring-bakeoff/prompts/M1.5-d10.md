# Authoring bake-off prompt — M1.5 (text, changed defs only) — density d10 (E1–E10)

## Shared instruction block
You are authoring code in beagle, a Clojure-subset language with explicit type
annotations (`:- Type`).

You will be given: (1) a **task**, (2) the **current program state** (empty, or the
existing program). Produce output that satisfies the task and nothing more. A
fail-closed engine will compile, type-check, and run your output against a hidden
test vector. If it fails, you receive a structured error report
(`<file>:<line>: beagle: <reason>`) and may revise. You have ‹R› revision rounds
and a ‹TOKEN_BUDGET› ceiling, whichever binds first.

Rules: output **only** the artifact specified by your output contract — no prose,
no fences. Do not invent operations, predicates, parameters, or types not part of
beagle; if unsure an element exists, do not use it. For edit tasks, preserve the
behavior of every definition the task does not ask you to change.

## Output contract (M1.5 — text, changed definitions only)
Emit beagle source **text**, but **only the definitions you ADD or CHANGE** — do
NOT re-emit unchanged definitions. The harness applies your output at
definition granularity: each `defn`/`defrecord`/`def` you emit REPLACES the
existing definition of that name (or is added if new); every definition you do
NOT emit is preserved untouched. So for an additive task emit just the new
definition; for an edit emit just the changed definition. (Like M1 this is text
with spelling-based references — if you rename a definition, every use site that
you DO emit must use the new spelling.)

## Examples (E1–E10) — verified [task/state/answer]

### E1
TASK: Author `square`: square of an Int.
STATE:
```
(empty program)
```
ANSWER:
```
(defn square [x :- Int] :- Int (* x x))
```
(defn, unary param, binary op body. The canonical leaf+list shape.)

### E2
TASK: Author `inc`: add one to an Int.
STATE:
```
(empty program)
```
ANSWER:
```
(defn inc [x :- Int] :- Int (+ x 1))
```
(A literal operand: `1` is kind=number, v="1" (T1). Distinct from add (binary).)

### E3
TASK: The program has `inc`. Add `dec` (subtract one), without changing `inc`.
STATE:
```
(defn inc [x :- Int] :- Int (+ x 1))   ; nodes @m#1..@m#14 as in E2
```
ANSWER:
```
(defn dec [x :- Int] :- Int (- x 1))   ; inc reproduced verbatim alongside
```
(PURE ADDITIVE delta: a fresh subtree (new node-ids only) + the wrapper append (engine adds the f<path>~<tie> order key). NO node of `inc` is touched -> no collateral damage.)

### E4
TASK: Author `max2`: the larger of two Ints.
STATE:
```
(empty program)
```
ANSWER:
```
(defn max2 [a :- Int b :- Int] :- Int (if (> a b) a b))
```
(CONDITIONAL: `if` is NOT a special kind — it is a plain `list` headed by the symbol `if`: f0=if, f1=cond, f2=then, f3=else. (Resolves the 'if/cond/then/else INVENTED' placeholder: there is no such vocabulary.))

### E5
TASK: Author a record `Point` with Int fields x and y.
STATE:
```
(empty program)
```
ANSWER:
```
(defrecord Point [x :- Int y :- Int])
```
(RECORD: `defrecord` is NOT a special kind — a `list` headed by `defrecord`; the field vector is the SAME `#%brackets` list with `field :- Type` slots as a defn param vector. (Resolves the 'record/field INVENTED' placeholder.))

### E6
TASK: Rename the function `inc` to `bump`. (Its uses must follow.)
STATE:
```
(defn inc [x :- Int] :- Int (+ x 1))   ; binding leaf @m#3, v="inc"
(defn double-inc [x :- Int] :- Int (inc (inc x)))  ; two uses of inc: leaves @m#17, @m#19
```
ANSWER:
```
(defn bump [x :- Int] :- Int (+ x 1))
(defn double-inc [x :- Int] :- Int (bump (bump x)))   ; BOTH call sites re-spelled
```
(SUBSTITUTION (logged): the original E6 was a PARAM rename; the engine `rename` verb operates on a DEF binding only (no param-rename verb), so this is a DEF rename — exercising the IDENTICAL failure mode (reference-follows-identity, shadow-correct). M2 re-spells ONE binding leaf; the engine first materializes the durable `bound_to` edges for the two uses, which then render `bump` for free. 2 effective ops, the uses are NOT edited. ENGINE-DUMPED.)

### E7
TASK: Author `sum3`: the sum of three Ints, as nested binary `+`.
STATE:
```
(empty program)
```
ANSWER:
```
(defn sum3 [a :- Int b :- Int c :- Int] :- Int (+ (+ a b) c))
```
(REFERENCE/NESTING DEPTH (M2's stress point): the inner (+ a b) is itself a `list` node @m#19 wired as f1 of the outer + call. Pure recursion of the list shape.)

### E8
TASK: Change the BODY of `negate` from (- 0 n) to (* -1 n). Leave its signature (name, param, return type) unchanged.
STATE:
```
(defn negate [n :- Int] :- Int (- 0 n))   ; defn @m#17, body slot is f5 = @m#27
```
ANSWER:
```
(defn negate [n :- Int] :- Int (* -1 n))   ; re-emit the defn with the new body
```
(SUBSTITUTION (logged): original E8 was a return-type supersede `[@id ret Nat]`. That is a FANTASY — there is no `ret` predicate and no return-type/signature verb; a return type is an fN SLOT, only changeable by re-minting the form (upsert-replace, ~40 ops) OR via a body edit. Worse, `Nat` is not a valid beagle type (build-rejects). Substituted to a `set-body` edit — the engine's GENUINE surgical edit: retire the old body edge, mint the new body subtree, wire it as f5. Signature/name/params untouched. 12 ops. ENGINE-DUMPED.)

### E9
TASK: The program has `inc`. Author `double-inc` which applies `inc` twice.
STATE:
```
(defn inc [x :- Int] :- Int (+ x 1))   ; defn @m#1, name binding leaf @m#3
```
ANSWER:
```
(defn double-inc [x :- Int] :- Int (inc (inc x)))
```
(CROSS-DEFINITION CALL: the callee `inc` is just a symbol leaf (v="inc", @m#52/@m#54) — there is NO `callee` predicate. Resolution binds it to the `inc` DEFINITION via bound_to. (Resolves the 'callee INVENTED' placeholder.))

### E10
TASK: The program has the record `Point`. Add `origin?`, true when both fields are 0.
STATE:
```
(defrecord Point [x :- Int y :- Int])   ; record def @m#1, type-name leaf @m#3 v="Point"
```
ANSWER:
```
(defn origin? [p :- Point] :- Bool (and (= (:x p) 0) (= (:y p) 0)))
```
(ADDITIVE on a record program (multi-element delta, no collateral). FIELD-ACCESS `(:x p)` is NOT an `access/of/field` node — it is a plain `list` whose head is the accessor leaf `:x` (kind=SYMBOL, v=":x" — the colon is retained in the spelling, the same rule as `:-`) applied to `p`. (Resolves the 'access INVENTED' placeholder.))
